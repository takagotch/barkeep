### barkeep
---
https://github.com/ooyala/barkeep

```
CONFIGURE_OPTS="--with-openssl-dir=/opt/local" rbenv install 1.9.3-p194
rvm install ruby-1.9.3-p194

sudo port install redis
brew install redis

port install node_js

gem install bundler
./script/initial_app_setup.rb

sudo apt-get update
sudo apt-get install -y mysql-server mysql-client libmysqlclietn-dev openssl libopenssl-ruby \

sudo apt-get install python-software-properties
sudo add-apt-repository ppa:git-core/ppa
sudo apt-get update
sudo apt-get install -y git

gem install bundeler
./script/initial_app_setup.rb


```

```
```

```
```


